{
  "name": "electron-with-create-react-app",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "electron": "^1.7.9",
    "foreman": "^2.0.0",
    "react-scripts": "0.8.5"
  },
  "dependencies": {
    "react": "^16.1.1",
    "react-dom": "^16.1.1",
    "react-router-dom": "^4.2.2",
    "semantic-ui": "^2.3.1",
    "semantic-ui-react": "^0.79.0"
  },
  "homepage": "./",
  "main": "src/electron-starter.js",
  "scripts": {
    "start": "npm run build-semantic && npm run build-styles && nf start -p 3000",
    "build": "npm run build-semantic && npm run build-styles && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "electron": "electron .",
    "electron-start": "node src/electron-wait-react",
    "react-start": "react-scripts start",
    "build-semantic": "cd src/semantic/ && gulp build",
    "build-styles": "cd src/styles/ && gulp build"
  }
}
